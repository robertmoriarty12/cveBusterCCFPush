# Architecture Diagram - CCF Push Connector

## High-Level Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER INTERACTION                            │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    config_gui.py (GUI)                          │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Configuration Form:                                       │ │
│  │  • Tenant ID                                              │ │
│  │  • Application ID                                         │ │
│  │  • Application Secret                                     │ │
│  │  • DCE Endpoint                                           │ │
│  │  • DCR Immutable ID                                       │ │
│  │  • Stream Name                                            │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  [💾 Save Configuration]  [🚀 Manual Send]                     │
└────────┬─────────────────────────┬──────────────────────────────┘
         │                         │
         │ Saves                   │ Triggers
         ▼                         ▼
┌──────────────────────┐   ┌──────────────────────┐
│connector_config.json │   │  generate_data.py    │
│ (Azure Settings)     │   │  (Test Data Creator) │
└──────────────────────┘   └──────┬───────────────┘
                                  │ Creates
                                  ▼
                          ┌──────────────────────┐
                          │cvebuster_data.json   │
                          │ (500 CVE records)    │
                          └──────┬───────────────┘
                                 │
                                 │ Read by
                                 ▼
                          ┌──────────────────────┐
                          │  send_to_azure.py    │
                          │  (Azure Ingestion)   │
                          └──────┬───────────────┘
                                 │
                                 │ 1. Authenticate
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Azure Active Directory                        │
│  ClientSecretCredential:                                        │
│  • Tenant ID + Client ID + Client Secret                        │
│  • Returns: OAuth 2.0 Bearer Token                             │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 2. Send with Token
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│            Data Collection Endpoint (DCE)                        │
│  POST https://xxx.ingest.monitor.azure.com/                     │
│       dataCollectionRules/{DCR_ID}/streams/{STREAM}             │
│  Headers: Authorization: Bearer {token}                         │
│  Body: JSON array of records                                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 3. Apply Rules
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              Data Collection Rule (DCR)                          │
│  • Validates schema                                             │
│  • Transforms fields                                            │
│  • Routes to destination                                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 4. Ingest
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              Log Analytics Workspace                             │
│  Custom Table: cveBusterv2_CL                                   │
│  • Data available in 5-10 minutes                               │
│  • Query with KQL: cveBusterv2_CL | take 100                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 5. Analyze
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Microsoft Sentinel                            │
│  • Analytics Rules                                              │
│  • Incidents & Alerts                                           │
│  • Workbooks & Dashboards                                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow Detail

```
send_to_azure.py
       │
       │ 1. Create Credential
       ▼
ClientSecretCredential(
   tenant_id,
   client_id,
   client_secret
)
       │
       │ 2. Request Token
       ▼
POST https://login.microsoftonline.com/{tenant}/oauth2/v2.0/token
Body: 
   grant_type=client_credentials
   client_id={client_id}
   client_secret={client_secret}
   scope=https://monitor.azure.com/.default
       │
       │ 3. Receive Token
       ▼
Response:
{
   "access_token": "eyJ0eXAiOiJKV1...",
   "expires_in": 3599,
   "token_type": "Bearer"
}
       │
       │ 4. Cached & Auto-refreshed by SDK
       ▼
LogsIngestionClient
   uses token automatically
```

---

## Data Flow Detail

```
generate_data.py creates:
┌─────────────────────────────────────────┐
│ cvebuster_data.json (500 records)       │
│ [                                       │
│   {                                     │
│     "VulnId": "CVE-2024-10001",        │
│     "VulnTitle": "...",                │
│     "Severity": "Critical",            │
│     "CVSS": 9.8,                       │
│     "MachineName": "SRV-WEB-001",      │
│     "LastModified": "2024-11-06T..."   │
│   },                                    │
│   {...}, {...}, ...                     │
│ ]                                       │
└─────────────────────────────────────────┘
                 │
                 │ Read & Batch (25 records)
                 ▼
        send_to_azure.py
                 │
                 │ Batch 1 (records 0-24)
                 ├──────────► POST to DCE ──► 200 OK
                 │
                 │ Batch 2 (records 25-49)
                 ├──────────► POST to DCE ──► 200 OK
                 │
                 │ ... (20 batches total)
                 │
                 │ Batch 20 (records 475-499)
                 └──────────► POST to DCE ──► 200 OK
```

---

## File Interaction Diagram

```
┌────────────────┐
│  setup.sh      │
│  (Run once)    │
└────────┬───────┘
         │ Installs dependencies
         ▼
┌────────────────────────────────────────┐
│ System Ready                           │
│ • Python 3 installed                   │
│ • tkinter installed                    │
│ • azure-identity installed             │
│ • azure-monitor-ingestion installed    │
└────────────────────────────────────────┘
         │
         │ Now you can run:
         ▼
┌────────────────┐      ┌────────────────┐
│ config_gui.py  │  OR  │ demo_mode.py   │
│ (Real Azure)   │      │ (Fake Azure)   │
└────────┬───────┘      └────────────────┘
         │
         │ Both use:
         ▼
┌────────────────┐      ┌────────────────┐
│generate_data.py│      │send_to_azure.py│
│ (Creates test  │      │ (Sends to DCE) │
│  CVE data)     │      │                │
└────────────────┘      └────────────────┘
         │                       │
         │ Creates               │ Reads
         ▼                       ▼
┌────────────────────────────────────────┐
│ cvebuster_data.json                    │
│ (500 CVE records in JSON array)        │
└────────────────────────────────────────┘
                 │
                 │ Also reads:
                 ▼
┌────────────────────────────────────────┐
│ connector_config.json                  │
│ {                                      │
│   "tenant_id": "...",                  │
│   "application_id": "...",             │
│   "application_secret": "...",         │
│   "dce_endpoint": "https://...",       │
│   "dcr_immutable_id": "dcr-...",       │
│   "stream_name": "Custom-..."          │
│ }                                      │
└────────────────────────────────────────┘
```

---

## Comparison: CCF Poll vs CCF Push

### CCF POLL (cveBusterCCFPaging-Pull)
```
┌─────────────────┐         ┌─────────────────┐
│  Flask Server   │◄────────┤  Microsoft      │
│  (Your API)     │  HTTP   │  Sentinel       │
│  Port 5000      │  GET    │  (Polls you)    │
└─────────────────┘         └─────────────────┘
        │
        │ Reads from:
        ▼
┌─────────────────┐
│cvebuster_data   │
│.json            │
└─────────────────┘

Direction: Sentinel → You
Trigger: Every 5 minutes (Sentinel's schedule)
Auth: API Key
```

### CCF PUSH (This Project)
```
┌─────────────────┐         ┌─────────────────┐
│  Python Script  │────────►│  Microsoft      │
│  send_to_azure  │  HTTP   │  Sentinel       │
│                 │  POST   │  (Receives)     │
└─────────────────┘         └─────────────────┘
        │
        │ Reads from:
        ▼
┌─────────────────┐
│cvebuster_data   │
│.json            │
└─────────────────┘

Direction: You → Sentinel
Trigger: Manual / Event-driven (your schedule)
Auth: OAuth 2.0 Client Secret
```

---

## Component Responsibilities

```
┌─────────────────────────────────────────────────────────┐
│                     config_gui.py                        │
│  Responsibilities:                                       │
│  ✓ Display UI form                                      │
│  ✓ Validate user input                                  │
│  ✓ Save/load configuration                              │
│  ✓ Orchestrate generate → send workflow                 │
│  ✓ Display activity logs                                │
│  ✓ Handle button clicks                                 │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   send_to_azure.py                       │
│  Responsibilities:                                       │
│  ✓ Authenticate with Azure AD                           │
│  ✓ Create LogsIngestionClient                           │
│  ✓ Batch data (25 records per batch)                    │
│  ✓ Send HTTP POST to DCE                                │
│  ✓ Handle errors & retries                              │
│  ✓ Report success/failure statistics                    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   generate_data.py                       │
│  Responsibilities:                                       │
│  ✓ Generate 500 CVE records                             │
│  ✓ Randomize field values (severity, CVSS, etc.)        │
│  ✓ Create realistic timestamps (30% recent, 70% old)    │
│  ✓ Write to cvebuster_data.json                         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                      setup.sh                            │
│  Responsibilities:                                       │
│  ✓ Install system packages (python3, tkinter)           │
│  ✓ Install Python packages (azure-*, etc.)              │
│  ✓ Set file permissions (chmod +x)                      │
│  ✓ Verify installation                                  │
└─────────────────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
send_to_azure.py attempts to send batch:
          │
          ▼
     ┌─────────┐
     │ Try POST│
     └────┬────┘
          │
    ┌─────┴──────┐
    │            │
 Success?    Failed?
    │            │
    ▼            ▼
┌────────┐   ┌──────────────────┐
│Log OK  │   │ Check error code │
│Continue│   └────────┬─────────┘
└────────┘            │
              ┌───────┴────────┐
              │                │
       429,500,502,503,504   Other
       (Transient errors)    (Fatal)
              │                │
              ▼                ▼
         ┌─────────┐      ┌────────┐
         │Sleep 2s │      │Log err │
         │& Retry  │      │Stop    │
         └────┬────┘      └────────┘
              │
        ┌─────┴──────┐
        │            │
    Success?     Failed?
        │            │
        ▼            ▼
   ┌────────┐   ┌────────┐
   │Log OK  │   │Log err │
   │Continue│   │Stop    │
   └────────┘   └────────┘
```

---

## Production Deployment Options

### Option 1: Systemd Service (Recommended)
```
┌─────────────────────────────────────────────┐
│ /etc/systemd/system/cvebuster-push.service │
│                                             │
│ [Service]                                   │
│ ExecStart=/usr/bin/python3                  │
│   /opt/cvebuster/send_to_azure.py           │
│   --config /etc/cvebuster/config.json       │
│   --data /var/lib/cvebuster/data.json       │
│                                             │
│ [Timer]                                     │
│ OnCalendar=*:0/5  # Every 5 minutes         │
└─────────────────────────────────────────────┘
```

### Option 2: Cron Job
```
# /etc/cron.d/cvebuster-push
*/5 * * * * cvebuster /usr/bin/python3 \
  /opt/cvebuster/send_to_azure.py \
  --config /etc/cvebuster/config.json \
  --data /var/lib/cvebuster/data.json
```

### Option 3: Event-Driven (Webhook)
```
┌─────────────────┐         ┌──────────────────┐
│ Scanner detects │────────►│ Webhook endpoint │
│ new CVE         │  HTTP   │ (Flask/FastAPI)  │
└─────────────────┘  POST   └────────┬─────────┘
                                     │
                                     │ Triggers
                                     ▼
                            ┌──────────────────┐
                            │ send_to_azure.py │
                            │ (Immediate push) │
                            └──────────────────┘
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    SECRETS STORAGE                       │
└────┬────────────────────────────────────────────────────┘
     │
     ├── Development: connector_config.json (local file)
     │   ⚠️ Not secure, for testing only
     │
     ├── Production Option 1: Environment Variables
     │   export AZURE_TENANT_ID=...
     │   export AZURE_CLIENT_ID=...
     │   export AZURE_CLIENT_SECRET=...
     │
     ├── Production Option 2: Azure Key Vault
     │   from azure.keyvault.secrets import SecretClient
     │   secret = client.get_secret("azure-client-secret")
     │
     └── Production Option 3: Managed Identity
         (No secrets needed if running in Azure)
         from azure.identity import ManagedIdentityCredential
```

---

This architecture demonstrates the CCF Push pattern for Microsoft Sentinel,
where your application pushes data to Azure rather than being polled.
